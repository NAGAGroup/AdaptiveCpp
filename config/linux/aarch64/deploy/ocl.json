{
  "internal": [
    {
      "src": "{{ toolchain-path }}/{{ acpp-libdir }}/hipSYCL",
      "dest": "{{ acpp-libdir }}/hipSYCL",
      "files": ["SHARED_LIB:rt-backend-ocl"]
    },
    {
      "src": "{{ toolchain-path }}/{{ acpp-libdir }}/hipSYCL/bitcode",
      "dest": "{{ acpp-libdir }}/hipSYCL/bitcode",
      "files": ["libkernel-sscp-spirv-full.bc"]
    }
  ],
  "external-permissive": [
    {
      "src": "{{ ocl-lib-path }}",
      "dest": "{{ ocl-deploy-path }}/{{ ocl-libdir }}",
      "files": ["SHARED_LIB:OpenCL"]
    }
  ]
}
